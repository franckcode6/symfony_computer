{% extends 'base.html.twig' %}

{% block title %} Nouveau composant
{% endblock %}

{% block body %}
<h2>Ajouter un periphérique</h2>
	{{ form_start(formComponent) }}
	{{ form_rest(formComponent) }}

	<button type="submit" class="btn btn-success">Valider</button>

	{{ form_end(formComponent) }}
	<br>
	<a href=" {{ path('component_index') }}" class="btn btn-success">Liste des périphériques</a>
	{% endblock %}
